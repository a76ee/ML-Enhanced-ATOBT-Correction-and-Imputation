# Private Training Data

Put local training files here when running the app or reproduction scripts.

Do not upload raw training or test data to GitHub. This directory ignores `.csv`, `.xlsx`, and `.xlsm` files by default.

